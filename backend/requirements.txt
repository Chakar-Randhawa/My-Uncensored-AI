fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
sse-starlette==2.1.3
pydantic==2.9.2
pydantic-settings==2.5.2
pyjwt[crypto]==2.9.0
python-multipart==0.0.9

# Semantic response cache — ONNX embeddings, no PyTorch/GPU required.
fastembed==0.3.6

# Observability (optional — only activates if SENTRY_DSN is set).
sentry-sdk[fastapi]==2.15.0

# Test suite
pytest==8.3.3
pytest-asyncio==0.24.0
